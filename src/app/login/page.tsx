import { LoginForm } from '@/components/auth/LoginForm';

export const metadata = {
    title: 'Login | DSD Finance Hub',
    description: 'Sign in to DSD Finance Hub',
};

export default function LoginPage() {
    return <LoginForm />;
}
