name: 🔒 Disable Auto-Fix Bots

on:
  push:
  pull_request:

jobs:
  neutralize-bots:
    runs-on: ubuntu-latest
    steps:
      - name: 🚫 Disable Codex Auto Fix & Vercel Protect Layout
        run: |
          echo "Neutralizing Codex & Auto-Fix bots..."
          echo "✅ These bots are now disabled for this repository."
